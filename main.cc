#include <iostream>
using namespace std;

int main ()

{
	if (Comms.init() != 0) {return -1;}
	
	while (1)
	{
		//poll everything
		//decide if we need to jump to next state
		//decide based on state what to do
		//do it
	}	

}

